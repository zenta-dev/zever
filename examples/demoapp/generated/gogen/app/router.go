// Code generated by the zever DSL gogen backend. DO NOT EDIT.

// Package app holds the generated HTTP routing for the app module.
package app

import (
	"encoding/json"
	"errors"
	"github.com/zenta-dev/zever/core/auth"
	"github.com/zenta-dev/zever/core/authz"
	"github.com/zenta-dev/zever/core/permission"
	"github.com/zenta-dev/zever/shared/apperror"
	"google.golang.org/protobuf/encoding/protojson"
	"google.golang.org/protobuf/proto"
	"io"
	"net/http"

	"github.com/zenta-dev/zever/core/router"
)

const maxRequestBodyBytes = 10 << 20 // 10MB, matches document/local.maxSourceBytes

// handleUserServiceGetUser handles GET /v1/users/{id}, calling UserService.GetUser.
func handleUserServiceGetUser(svc UserService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &GetUserRequest{}

		{
			raw := router.Param(r, "id")
			if raw == "" {
				writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required parameter \"id\""))
				return
			}

			req.Id = raw

		}

		resp, err := svc.GetUser(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// handleUserServiceListUsers handles GET /v1/users, calling UserService.ListUsers.
func handleUserServiceListUsers(svc UserService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &ListUsersRequest{}

		resp, err := svc.ListUsers(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// RegisterUserServiceRoutes wires every HTTP-transport operation of UserService onto r,
// calling svc for each request. An operation with an auth:/permission:
// declaration is wrapped in authz.Middleware, enforcing the identical
// policy the same operation's gRPC method enforces via GRPCPolicies() --
// see authz.Authorize, the single implementation both transports call.
func RegisterUserServiceRoutes(r router.Router, svc UserService, a auth.Auth, p permission.Checker) {
	r.Handle("GET", "/v1/users/{id}", authz.Middleware(a, p, UserServiceGetUserPolicy, nil)(handleUserServiceGetUser(svc)).ServeHTTP)
	r.Handle("GET", "/v1/users", authz.Middleware(a, p, UserServiceListUsersPolicy, nil)(handleUserServiceListUsers(svc)).ServeHTTP)
}

// handleProductServiceGetProduct handles GET /v1/products/{id}, calling ProductService.GetProduct.
func handleProductServiceGetProduct(svc ProductService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &GetProductRequest{}

		{
			raw := router.Param(r, "id")
			if raw == "" {
				writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required parameter \"id\""))
				return
			}

			req.Id = raw

		}

		resp, err := svc.GetProduct(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// handleProductServiceListProducts handles GET /v1/products, calling ProductService.ListProducts.
func handleProductServiceListProducts(svc ProductService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &ListProductsRequest{}

		{
			raw := r.URL.Query().Get("category_id")
			if raw == "" {
				writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required parameter \"category_id\""))
				return
			}

			req.CategoryId = raw

		}

		resp, err := svc.ListProducts(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// handleProductServiceCreateProduct handles POST /v1/products, calling ProductService.CreateProduct.
func handleProductServiceCreateProduct(svc ProductService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &CreateProductRequest{}

		body, err := io.ReadAll(http.MaxBytesReader(w, r.Body, maxRequestBodyBytes))
		if err != nil {
			msg := "invalid JSON body"

			var maxErr *http.MaxBytesError
			if errors.As(err, &maxErr) {
				msg = "request body too large"
			}

			writeGogenError(w, apperror.New(apperror.InvalidArgument, msg))
			return
		}

		if err := protojson.Unmarshal(body, req); err != nil {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "invalid JSON body"))
			return
		}

		if req.Name == "" {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required field \"name\""))
			return
		}

		resp, err := svc.CreateProduct(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// RegisterProductServiceRoutes wires every HTTP-transport operation of ProductService onto r,
// calling svc for each request. An operation with an auth:/permission:
// declaration is wrapped in authz.Middleware, enforcing the identical
// policy the same operation's gRPC method enforces via GRPCPolicies() --
// see authz.Authorize, the single implementation both transports call.
func RegisterProductServiceRoutes(r router.Router, svc ProductService, a auth.Auth, p permission.Checker) {
	r.Handle("GET", "/v1/products/{id}", authz.Middleware(a, p, ProductServiceGetProductPolicy, nil)(handleProductServiceGetProduct(svc)).ServeHTTP)
	r.Handle("GET", "/v1/products", authz.Middleware(a, p, ProductServiceListProductsPolicy, nil)(handleProductServiceListProducts(svc)).ServeHTTP)
	r.Handle("POST", "/v1/products", authz.Middleware(a, p, ProductServiceCreateProductPolicy, nil)(handleProductServiceCreateProduct(svc)).ServeHTTP)
}

// handleOrderServiceGetOrder handles GET /v1/orders/{id}, calling OrderService.GetOrder.
func handleOrderServiceGetOrder(svc OrderService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &GetOrderRequest{}

		{
			raw := router.Param(r, "id")
			if raw == "" {
				writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required parameter \"id\""))
				return
			}

			req.Id = raw

		}

		resp, err := svc.GetOrder(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// handleOrderServiceCreateOrder handles POST /v1/orders, calling OrderService.CreateOrder.
func handleOrderServiceCreateOrder(svc OrderService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &CreateOrderRequest{}

		body, err := io.ReadAll(http.MaxBytesReader(w, r.Body, maxRequestBodyBytes))
		if err != nil {
			msg := "invalid JSON body"

			var maxErr *http.MaxBytesError
			if errors.As(err, &maxErr) {
				msg = "request body too large"
			}

			writeGogenError(w, apperror.New(apperror.InvalidArgument, msg))
			return
		}

		if err := protojson.Unmarshal(body, req); err != nil {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "invalid JSON body"))
			return
		}

		if req.ProductIds == "" {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required field \"product_ids\""))
			return
		}

		resp, err := svc.CreateOrder(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// RegisterOrderServiceRoutes wires every HTTP-transport operation of OrderService onto r,
// calling svc for each request. An operation with an auth:/permission:
// declaration is wrapped in authz.Middleware, enforcing the identical
// policy the same operation's gRPC method enforces via GRPCPolicies() --
// see authz.Authorize, the single implementation both transports call.
func RegisterOrderServiceRoutes(r router.Router, svc OrderService, a auth.Auth, p permission.Checker) {
	r.Handle("GET", "/v1/orders/{id}", authz.Middleware(a, p, OrderServiceGetOrderPolicy, nil)(handleOrderServiceGetOrder(svc)).ServeHTTP)
	r.Handle("POST", "/v1/orders", authz.Middleware(a, p, OrderServiceCreateOrderPolicy, nil)(handleOrderServiceCreateOrder(svc)).ServeHTTP)
}

// writeGogenJSON writes v as a plain (non-protobuf) JSON response body with
// the given status -- used only for the generic error shape below, since an
// *apperror.Error is not a protobuf message.
func writeGogenJSON(w http.ResponseWriter, status int, v any) {
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(status)
	_ = json.NewEncoder(w).Encode(v)
}

// writeGogenProtoJSON writes msg -- a real protobuf message -- as a
// protojson-encoded response body with the given status. protojson is
// protobuf's canonical JSON mapping: field names render in lowerCamelCase
// (e.g. "userId"), not this backend's earlier plain-struct casing.
func writeGogenProtoJSON(w http.ResponseWriter, status int, msg proto.Message) {
	body, err := protojson.Marshal(msg)
	if err != nil {
		writeGogenJSON(w, http.StatusInternalServerError, map[string]string{"error": "internal error"})
		return
	}

	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(status)
	_, _ = w.Write(body)
}

// writeGogenError maps err to an HTTP status: an *apperror.Error maps via
// its Code.HTTPStatus(); any other error maps to 500.
func writeGogenError(w http.ResponseWriter, err error) {
	var appErr *apperror.Error
	if errors.As(err, &appErr) {
		writeGogenJSON(w, appErr.Code().HTTPStatus(), map[string]string{"error": appErr.Message()})
		return
	}

	writeGogenJSON(w, http.StatusInternalServerError, map[string]string{"error": "internal error"})
}
