// Code generated by the zever DSL gogen backend. DO NOT EDIT.

// Package app holds the generated HTTP routing for the app module.
package app

import (
	"encoding/json"
	"errors"
	"github.com/zenta-dev/zever/core/auth"
	"github.com/zenta-dev/zever/core/authz"
	"github.com/zenta-dev/zever/core/permission"
	"github.com/zenta-dev/zever/shared/apperror"
	"google.golang.org/protobuf/encoding/protojson"
	"google.golang.org/protobuf/proto"
	"io"
	"net/http"

	"github.com/zenta-dev/zever/core/router"
)

const maxRequestBodyBytes = 10 << 20 // 10MB, matches document/local.maxSourceBytes

// handleBookingServiceCreateSpace handles POST /spaces, calling BookingService.CreateSpace.
func handleBookingServiceCreateSpace(svc BookingService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &CreateSpaceRequest{}

		body, err := io.ReadAll(http.MaxBytesReader(w, r.Body, maxRequestBodyBytes))
		if err != nil {
			msg := "invalid JSON body"

			var maxErr *http.MaxBytesError
			if errors.As(err, &maxErr) {
				msg = "request body too large"
			}

			writeGogenError(w, apperror.New(apperror.InvalidArgument, msg))
			return
		}

		if err := protojson.Unmarshal(body, req); err != nil {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "invalid JSON body"))
			return
		}

		if req.Title == "" {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required field \"title\""))
			return
		}

		if req.Description == "" {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required field \"description\""))
			return
		}

		resp, err := svc.CreateSpace(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// handleBookingServiceListSpaces handles GET /spaces, calling BookingService.ListSpaces.
func handleBookingServiceListSpaces(svc BookingService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &ListSpacesRequest{}

		resp, err := svc.ListSpaces(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// handleBookingServiceGetSpace handles GET /spaces/{id}, calling BookingService.GetSpace.
func handleBookingServiceGetSpace(svc BookingService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &GetSpaceRequest{}

		{
			raw := router.Param(r, "id")
			if raw == "" {
				writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required parameter \"id\""))
				return
			}

			req.Id = raw

		}

		resp, err := svc.GetSpace(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// handleBookingServiceCreateBooking handles POST /bookings, calling BookingService.CreateBooking.
func handleBookingServiceCreateBooking(svc BookingService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &CreateBookingRequest{}

		body, err := io.ReadAll(http.MaxBytesReader(w, r.Body, maxRequestBodyBytes))
		if err != nil {
			msg := "invalid JSON body"

			var maxErr *http.MaxBytesError
			if errors.As(err, &maxErr) {
				msg = "request body too large"
			}

			writeGogenError(w, apperror.New(apperror.InvalidArgument, msg))
			return
		}

		if err := protojson.Unmarshal(body, req); err != nil {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "invalid JSON body"))
			return
		}

		if req.SpaceId == "" {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required field \"space_id\""))
			return
		}

		if req.StartDate == "" {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required field \"start_date\""))
			return
		}

		if req.EndDate == "" {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required field \"end_date\""))
			return
		}

		resp, err := svc.CreateBooking(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// handleBookingServiceCancelBooking handles DELETE /bookings/{id}, calling BookingService.CancelBooking.
func handleBookingServiceCancelBooking(svc BookingService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &CancelBookingRequest{}

		{
			raw := router.Param(r, "id")
			if raw == "" {
				writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required parameter \"id\""))
				return
			}

			req.Id = raw

		}

		resp, err := svc.CancelBooking(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// handleBookingServiceAddReview handles POST /reviews, calling BookingService.AddReview.
func handleBookingServiceAddReview(svc BookingService) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		ctx := r.Context()

		req := &AddReviewRequest{}

		body, err := io.ReadAll(http.MaxBytesReader(w, r.Body, maxRequestBodyBytes))
		if err != nil {
			msg := "invalid JSON body"

			var maxErr *http.MaxBytesError
			if errors.As(err, &maxErr) {
				msg = "request body too large"
			}

			writeGogenError(w, apperror.New(apperror.InvalidArgument, msg))
			return
		}

		if err := protojson.Unmarshal(body, req); err != nil {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "invalid JSON body"))
			return
		}

		if req.SpaceId == "" {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required field \"space_id\""))
			return
		}

		if req.Body == "" {
			writeGogenError(w, apperror.New(apperror.InvalidArgument, "missing required field \"body\""))
			return
		}

		resp, err := svc.AddReview(ctx, req)
		if err != nil {
			writeGogenError(w, err)
			return
		}

		writeGogenProtoJSON(w, http.StatusOK, resp)
	}
}

// RegisterBookingServiceRoutes wires every HTTP-transport operation of BookingService onto r,
// calling svc for each request. An operation with an auth:/permission:
// declaration is wrapped in authz.Middleware, enforcing the identical
// policy the same operation's gRPC method enforces via GRPCPolicies() --
// see authz.Authorize, the single implementation both transports call.
func RegisterBookingServiceRoutes(r router.Router, svc BookingService, a auth.Auth, p permission.Checker) {
	r.Handle("POST", "/spaces", authz.Middleware(a, p, BookingServiceCreateSpacePolicy, nil)(handleBookingServiceCreateSpace(svc)).ServeHTTP)
	r.Handle("GET", "/spaces", authz.Middleware(a, p, BookingServiceListSpacesPolicy, nil)(handleBookingServiceListSpaces(svc)).ServeHTTP)
	r.Handle("GET", "/spaces/{id}", authz.Middleware(a, p, BookingServiceGetSpacePolicy, nil)(handleBookingServiceGetSpace(svc)).ServeHTTP)
	r.Handle("POST", "/bookings", authz.Middleware(a, p, BookingServiceCreateBookingPolicy, nil)(handleBookingServiceCreateBooking(svc)).ServeHTTP)
	r.Handle("DELETE", "/bookings/{id}", authz.Middleware(a, p, BookingServiceCancelBookingPolicy, func(r *http.Request) string { return router.Param(r, "id") })(handleBookingServiceCancelBooking(svc)).ServeHTTP)
	r.Handle("POST", "/reviews", authz.Middleware(a, p, BookingServiceAddReviewPolicy, nil)(handleBookingServiceAddReview(svc)).ServeHTTP)
}

// writeGogenJSON writes v as a plain (non-protobuf) JSON response body with
// the given status -- used only for the generic error shape below, since an
// *apperror.Error is not a protobuf message.
func writeGogenJSON(w http.ResponseWriter, status int, v any) {
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(status)
	_ = json.NewEncoder(w).Encode(v)
}

// writeGogenProtoJSON writes msg -- a real protobuf message -- as a
// protojson-encoded response body with the given status. protojson is
// protobuf's canonical JSON mapping: field names render in lowerCamelCase
// (e.g. "userId"), not this backend's earlier plain-struct casing.
func writeGogenProtoJSON(w http.ResponseWriter, status int, msg proto.Message) {
	body, err := protojson.Marshal(msg)
	if err != nil {
		writeGogenJSON(w, http.StatusInternalServerError, map[string]string{"error": "internal error"})
		return
	}

	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(status)
	_, _ = w.Write(body)
}

// writeGogenError maps err to an HTTP status: an *apperror.Error maps via
// its Code.HTTPStatus(); any other error maps to 500.
func writeGogenError(w http.ResponseWriter, err error) {
	var appErr *apperror.Error
	if errors.As(err, &appErr) {
		writeGogenJSON(w, appErr.Code().HTTPStatus(), map[string]string{"error": appErr.Message()})
		return
	}

	writeGogenJSON(w, http.StatusInternalServerError, map[string]string{"error": "internal error"})
}
