// Code generated by the zever DSL zenorm backend. DO NOT EDIT.

// Package app holds the generated orm query-builder types for the application
// module's entities.
package app

import (
	"fmt"

	"github.com/zenta-dev/zever/orm"
)

// Employees is the orm.Table for Employee, generated from the "Employee" entity.
var Employees = orm.NewTable[Employee]("employees", []string{"id", "name", "title", "manager_id"})

// EmployeeCols holds typed orm.Column/orm.NullableColumn references for Employee,
// used to build type-safe predicates, order terms and assignments.
var EmployeeCols = struct {
	ID        orm.Column[Employee, string]
	Name      orm.Column[Employee, string]
	Title     orm.Column[Employee, string]
	ManagerID orm.NullableColumn[Employee, string]
}{
	ID:        orm.NewColumn[Employee, string]("employees", "id"),
	Name:      orm.NewColumn[Employee, string]("employees", "name"),
	Title:     orm.NewColumn[Employee, string]("employees", "title"),
	ManagerID: orm.NewNullableColumn[Employee, string]("employees", "manager_id"),
}

// Employee mirrors the "Employee" entity declared in the schema.
type Employee struct {
	ID        string             `json:"id"`
	Name      string             `json:"name"`
	Title     string             `json:"title"`
	ManagerID orm.Option[string] `json:"manager_id"`
}

// Scan reads one row, whose columns must be in Employees.Columns() order, into e.
func (e *Employee) Scan(row orm.Row) error {
	if err := row.Scan(&e.ID, &e.Name, &e.Title, &e.ManagerID); err != nil {
		return fmt.Errorf("[employee] scan error: %w", err)
	}

	return nil
}

// Columns lists Employee's columns in field-declaration order; Scan reads a
// row's columns in exactly this order.
func (Employee) Columns() []string { return []string{"id", "name", "title", "manager_id"} }

// EmployeeManagerRel is the "manager" relation declared on Employee, linking Employee.manager_id to
// Employee.id.
var EmployeeManagerRel = orm.NewRelation[Employee, Employee]("manager_id", "id", Employees)

// JoinEmployeeManager starts a typed INNER/LEFT JOIN from Employees through the "manager" relation,
// producing one orm.Row2 per matching row in a single round trip. See
// orm.Join2's doc comment for why joinType == orm.LeftJoin is only safe
// here when Employee has no non-nullable columns that could legitimately come
// back NULL -- prefer LeftJoinEmployeeManager for the general LEFT JOIN case.
func JoinEmployeeManager(left orm.Query[Employee, *Employee], joinType orm.JoinType) orm.Join2[Employee, *Employee, Employee, *Employee] {
	return orm.JoinOn(left, EmployeeManagerRel, joinType)
}

// LeftJoinEmployeeManager starts a null-safe LEFT JOIN from Employees through the "manager" relation: an
// unmatched Employee row comes back with an explicit orm.Option[Employee] rather
// than a zero-valued struct that could be mistaken for a real match.
func LeftJoinEmployeeManager(left orm.Query[Employee, *Employee]) orm.LeftJoin2[Employee, *Employee, Employee, *Employee] {
	return orm.LeftJoinOn(left, EmployeeManagerRel)
}

// EmployeeReportsRel is the "reports" relation declared on Employee, linking Employee.id to
// Employee.manager_id.
var EmployeeReportsRel = orm.NewRelation[Employee, Employee]("id", "manager_id", Employees)

// JoinEmployeeReports starts a typed INNER/LEFT JOIN from Employees through the "reports" relation,
// producing one orm.Row2 per matching row in a single round trip. See
// orm.Join2's doc comment for why joinType == orm.LeftJoin is only safe
// here when Employee has no non-nullable columns that could legitimately come
// back NULL -- prefer LeftJoinEmployeeReports for the general LEFT JOIN case.
func JoinEmployeeReports(left orm.Query[Employee, *Employee], joinType orm.JoinType) orm.Join2[Employee, *Employee, Employee, *Employee] {
	return orm.JoinOn(left, EmployeeReportsRel, joinType)
}

// LeftJoinEmployeeReports starts a null-safe LEFT JOIN from Employees through the "reports" relation: an
// unmatched Employee row comes back with an explicit orm.Option[Employee] rather
// than a zero-valued struct that could be mistaken for a real match.
func LeftJoinEmployeeReports(left orm.Query[Employee, *Employee]) orm.LeftJoin2[Employee, *Employee, Employee, *Employee] {
	return orm.LeftJoinOn(left, EmployeeReportsRel)
}
